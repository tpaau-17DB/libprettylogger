use crate::logging::*;
use crate::utils::{
    colors::*,
};

fn get_header(log_type: LogType) -> String {
    
}

fn get_color(log_type: LogType) -> String {

}

pub fn format_log() -> String {
    return format!("[]")
}
