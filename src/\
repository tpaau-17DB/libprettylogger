pub mod colors;

use colors::*;

pub fn log_debug(message: &String) {
    eprintln!("{}", RED.to_string() + message + RESET);
}

#[cfg(test)]
mod tests {
    use crate::*;
    #[test]
    fn it_works() {
        log = "this is a log";
        log_debug(&"This is a debug log!");
    }
}
